rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc + viết trong lúc test
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
